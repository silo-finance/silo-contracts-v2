methods {
    // function _.transferFrom(address, address, uint) external => DISPATCHER(true);
    // function _.transfer(address, uint) external => DISPATCHER(true);
    // function _.balanceOf(address) external => DISPATCHER(true);
    // function _.totalSupply() external => DISPATCHER(true);
    // function _.mint(uint256, address) external => DISPATCHER(true); //IERC4626
    // function _.decimals() external => DISPATCHER(true);
    // function _.name() external => DISPATCHER(true);
    // function _.allowance(address, address) external => DISPATCHER(true);
    // function _.approve(address, uint) external => DISPATCHER(true);
}