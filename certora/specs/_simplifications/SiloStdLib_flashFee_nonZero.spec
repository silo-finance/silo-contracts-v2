methods {
    function _.flashFee(address _config, address _token, uint256 _amount) external => simplified_flashFee(_config, _token, _amount) expect uint256;
}

function  simplified_flashFee(address _config, address _token, uint256 _amount) returns uint256 {
    uint256 fee;
    require fee != 0;
    return fee;
}
